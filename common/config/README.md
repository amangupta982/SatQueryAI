# Config

This folder contains components related to `config` for the `common` module.

**What belongs here**:
- Source code, scripts, or configurations specific to config.

**What should NOT be stored here**:
- Large datasets.
- Model weights or large checkpoint files.
- Compiled binaries.

**TODO**:
- [ ] Populate config with necessary implementations.
