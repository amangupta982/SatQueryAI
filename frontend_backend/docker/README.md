# Docker

This folder contains components related to `docker` for the `frontend_backend` module.

**What belongs here**:
- Source code, scripts, or configurations specific to docker.

**What should NOT be stored here**:
- Large datasets.
- Model weights or large checkpoint files.
- Compiled binaries.

**TODO**:
- [ ] Populate docker with necessary implementations.
